{
  "name": "xuanshen",
  "private": true,
  "version": "0.1.0",
  "description": "AI Desktop Pet",
  "scripts": {
    "dev": "pnpm --filter desktop-pet dev",
    "build": "pnpm --filter desktop-pet build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  }
}
